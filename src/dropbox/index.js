import * as dropboxSelectors from "./selectors";
import * as dropboxActions from "./actions";
import CloudFile from "./CloudFile";

export const selectors = dropboxSelectors;
export const actions = dropboxActions;
export const provider = CloudFile;
